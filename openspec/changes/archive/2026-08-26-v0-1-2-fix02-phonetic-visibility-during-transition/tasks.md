## 1. 实施方案 C：音标动画收归视图层

- [x] 1.1 `ClassicFadeEffect`：移除音标动画参与（guard 仅查单词标签、动画数组/模型写入仅单词图层、删除音标 wantsLayer）
- [x] 1.2 `CardFlipEffect`：同上（含中点音标 transform 复位移除）
- [x] 1.3 `WordTransitionEffect` 协议文档注释固化契约：动效 SHALL NOT 动画或改写音标图层
- [x] 1.4 单元测试：经典淡入/卡片翻转不向音标图层添加动画（`animation(forKey:)` 为 nil），单词图层动画正常存在
- [x] 1.5 构建：Debug build-for-testing 通过

## 2. 验证（用户在 Xcode 环境执行）

- [x] 2.1 注音"隐藏"模式：经典淡入/卡片翻转切词全程注音不可见
- [x] 2.2 注音"悬停显示"（鼠标在外）：同上
- [x] 2.3 注音"悬停显示"（鼠标在内）与"始终显示"：音标在动效中点瞬时切换（不随单词动画），全程可见无跳变
- [x] 2.4 预览：演示中注音可见、文本随演示切换
- [x] 2.5 其余 7 个动效回归：切词音标行为与修复前一致
