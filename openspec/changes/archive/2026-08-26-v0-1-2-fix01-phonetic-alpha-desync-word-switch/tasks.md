## 1. 修复实施

- [x] 1.1 `applyAlpha` 非动画路径显式同步 `layer.opacity`（附根因注释：alphaValue 缓存被动效显式动画绕过后去重跳写）
- [x] 1.2 构建：Debug build 通过

## 2. 验证

- [x] 2.1 注音"悬停显示"模式：鼠标在窗外切词后注音保持隐藏，移入浮现、移出淡出正常（用户在 Xcode 环境执行）
- [x] 2.2 注音"隐藏"模式：切词后注音保持不可见（用户在 Xcode 环境执行）
- [x] 2.3 释义同模式回归：释义"悬停显示/隐藏"下切词行为与注音一致（用户在 Xcode 环境执行）
- [x] 2.4 注音/释义"始终显示"模式：切词动效正常，无回归（用户在 Xcode 环境执行）
